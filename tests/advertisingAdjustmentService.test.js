const test=require('node:test');const assert=require('node:assert/strict');const{buildAdjustmentLogs}=require('../server/services/advertisingAdjustmentService');
test('requires a reason when protected quote prices change',()=>{assert.throws(()=>buildAdjustmentLogs({minimumOrderAmount:75},{minimumOrderAmount:50},'user-1',''),/原因/)});
test('records old and new values using authenticated actor',()=>{const logs=buildAdjustmentLogs({minimumProcessingFee:35,delivery:{saleUnitPrice:150}},{minimumProcessingFee:50,delivery:{saleUnitPrice:175}},'user-1','客户要求分批配送');assert.deepEqual(logs.map(x=>x.fieldName),['minimumProcessingFee','delivery.saleUnitPrice']);assert.ok(logs.every(x=>x.userId==='user-1'&&x.reason==='客户要求分批配送'))});
test('records item-level manual sales adjustments',()=>{const logs=buildAdjustmentLogs({items:[{id:'i1',materialSaleUnitPrice:20}]},{items:[{id:'i1',materialSaleUnitPrice:25}]},'admin-1','供应商涨价');assert.equal(logs[0].quoteItemId,'i1');assert.equal(logs[0].fieldName,'materialSaleUnitPrice')});
